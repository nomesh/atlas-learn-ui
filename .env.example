# ATLAS Learn Environment Configuration Template
VITE_API_BASE_URL=http://localhost:8080
VITE_USE_MOCK_API=true
VITE_APP_TITLE=ATLAS Learn
# Optional: Set tenant identity for local development (no silent defaults used)
VITE_ATLAS_TENANT_ID=
# Optional: Bearer JWT token for local development when testing against secured live backend
VITE_ATLAS_AUTH_TOKEN=

